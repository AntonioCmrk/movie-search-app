const Footer = () => {
  return (
    <footer className="mt-auto bg-gray-600 w-full p-7 flex justify-center items-center text-white">
      &#169; {new Date().getFullYear()} Movie Search app.
    </footer>
  );
};

export default Footer;
